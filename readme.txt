Hi! Thanks so much for commissioning me :)
Here's how to edit your website!

Changing Text:
Open index.html
To edit the header, locate what says "<h1>Neo's collection, A glimpse of the world</h1>
and edit what's in between the <h1> and </h1>

To edit the bio, locate what says "<p>My name is Neo!...</p>
and edit what's in between the <p> and </p>

Adding an image:
Make sure your image is in this file. 
HTML websites cannot access your computer's files other than the ones in this folder, so it *needs* to be here

Now, locate the gallery ("<section class="gallery">")
within the gallery and within the div, you should see several lines that start with "<img src="...""
to add a new file, make a new line underneath those and copy the exact same format.

What each one means:
img src basically links the image to the website, this is what makes the image show up
alt is 
data-caption is

to update your github repo, follow these exact steps.
in the top left corner of your screen, find the terminal tab. click it and click "new terminal."
(only if you don't have a terminal open)
where it says "powershell" in the top right of the terminal window, i recommend changing it to command prompt
type "git add ." EXACTLY and hit enter
then type "git commit -m "updated gallery" EXACTLY and hit enter
then type "git push" EXACTLY and hit enter. 

If you have any issues, please contact me! <3